# dotfiles

Run `./install.sh` script to install

Run `./install-skills.sh` to fetch Copilot skills from `github/graphql-platform`
and install them into `~/.copilot/skills`. See `./install-skills.sh --help` for options.
